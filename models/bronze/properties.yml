version: 2

models:
  - name: bronze_returns #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: view

  - name: bronze_sale
    config:
      materialized: view